{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx prisma@5.22.0 generate --schema=./packages/db/prisma/schema.prisma && cd packages/shared && npx tsc && cd ../../packages/db && npx tsc && cd ../../apps/api && npx nest build"
  },
  "deploy": {
    "startCommand": "cd apps/api && node dist/main",
    "healthcheckPath": "/api/v1/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
