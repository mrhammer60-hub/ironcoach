{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd packages/db && npx prisma generate && cd ../../packages/shared && npx tsc && cd ../db && npx tsc && cd ../../apps/api && npx nest build"
  },
  "deploy": {
    "startCommand": "cd apps/api && node dist/main",
    "restartPolicyType": "ON_FAILURE"
  }
}
