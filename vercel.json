{
  "buildCommand": "npx prisma@5.22.0 generate --schema=./packages/db/prisma/schema.prisma && cd apps/web && npx next build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs"
}
